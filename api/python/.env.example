# ICE Data Ingestion Environment Configuration

# ============================================================================
# LOCAL INGESTION MODE CONFIGURATION
# ============================================================================

# Local ingestion directory (where student/lead data and documents are stored)
LOCAL_INGESTION_DIR=/home/sebastiangarcia/ice-ingestion-data

# Ingestion mode: local, remote, or auto (auto-detects based on available sources)
INGESTION_MODE=auto

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL connection URL
DATABASE_URL=postgresql://user:password@localhost:5432/ice_database

# ============================================================================
# REMOTE INGESTION MODE CONFIGURATION (Google Drive)
# ============================================================================

# Google Drive credentials (JSON string)
GOOGLE_CREDENTIALS_JSON=

# Google Drive folder ID
GOOGLE_DRIVE_FOLDER_ID=

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file location (defaults to /var/log/ice_ingestion.log or ./logs/ice_ingestion.log)
# LOG_FILE=/var/log/ice_ingestion.log

# ============================================================================
# STORAGE ADAPTER CONFIGURATION (Future MinIO Integration)
# ============================================================================

# Storage mode: local, minio, s3 (currently only local is implemented)
STORAGE_MODE=local

# MinIO Configuration (Future)
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
# MINIO_BUCKET=ice-documents
# MINIO_SECURE=false
